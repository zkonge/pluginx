[package]
name = "client"
version = "0.1.0"
edition = "2021"

[dependencies]
futures-util = { version = "0.3.31", default-features = false }
pluginx = { path = "../../", features = ["client"] }
shared = { path = "../shared" }
tokio = { version = "1", features = ["full"] }
